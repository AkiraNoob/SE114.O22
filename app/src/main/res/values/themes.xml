<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.CollabTask" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="android:windowSplashScreenBackground">@color/primary</item>
        <item name="fontFamily">@font/font_family</item>
        <item name="android:textColor">@color/other</item>
        <item name="hintTextColor">@color/other</item>
        <item name="fontWeight">500</item>
        <item name="android:itemBackground">@color/secondary_light</item>
    </style>

    <style name="Theme.CollabTask" parent="Base.Theme.CollabTask" />

    <style name="roundedImageViewRounded">
        <item name="cornerFamily">rounded</item>
        <item name="cornerSize">50%</item>
    </style>
    <style name="fab_rounded">
        <item name="cornerFamily">rounded</item>
        <item name="cornerSize">100%</item>
    </style>
</resources>